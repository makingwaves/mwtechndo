@using EpiServer.AlloyDemo.GraphAPI
@using EPiServer.Core
@model IPageViewModel<GraphApiPage>

@{
    ViewBag.Title = "Graph API";
Layout = null;
}

<script>
    window.intranetSettings = {
        blocks: [
            @foreach (var block in Model.CurrentPage.MainContentArea.Items)
            {
                <text>"@block.GetContent().Name",</text>
            }
        ]
    }
</script>


@Html.Action("GetReactHtmlPage", "Graph")

